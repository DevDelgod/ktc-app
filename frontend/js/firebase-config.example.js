/**
 * Modelo de configuração do Firebase Web.
 *
 * COPIE este arquivo para `firebase-config.js` e preencha com os valores
 * do seu projeto:
 *
 *     Firebase Console > Configurações do projeto > Seus apps > ícone Web
 *
 * `firebase-config.js` está no .gitignore e nunca deve ser versionado.
 *
 * Nota: estas chaves do Firebase Web são públicas por natureza — elas
 * identificam o projeto, não autorizam acesso. Quem controla o acesso
 * são as Regras de Segurança do Firestore. Mantê-las fora do Git é
 * higiene de configuração, não proteção de segredo: a proteção real do
 * banco depende das regras estarem corretas.
 */

export const firebaseConfig = {
  apiKey: '',
  authDomain: 'SEU-PROJETO.firebaseapp.com',
  projectId: 'SEU-PROJETO',
  storageBucket: 'SEU-PROJETO.appspot.com',
  messagingSenderId: '',
  appId: '',
};
